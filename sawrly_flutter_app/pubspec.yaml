name: fotograf_mobile
description: "Sawrly صورلي - Iraqi photographers & clients platform. Enterprise M3 Material 3 Theme Engine (85 colors + seed harmonization + WCAG). Live polling från /api/config/public."
publish_to: 'none'
version: 1.2.15+210

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  dio: ^5.7.0
  url_launcher: ^6.3.1
  phosphor_flutter: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
